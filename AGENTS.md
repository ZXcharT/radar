# OneChart-Public 项目工作规则

本文件是 OneChart 公开静态站点仓库的项目级规则入口，只对本仓库生效。

## 1. 项目定位

本仓库是 OneChart 的公开部署镜像，保存浏览器可直接读取的 HTML、CSS、JavaScript、图片和公开数据文件。

私有计算逻辑、数据流水线、调度、发布生成工具及运维事实属于独立的私有 OneChart 工程，不得复制到本仓库。本仓库是公开产物的事实源，不是私有算法或原始数据的事实源。

## 2. 开工门禁

任何写入前核对：

```text
仓库绝对路径：
Git 根目录：
当前 branch：
当前 commit：
working tree：clean / dirty
本次基线：
任务类型：公开静态修复 / 已验证产物更新 / 文档维护
```

如果工作区存在无法解释的改动、目标不是本仓库唯一正式 Git 根，或公开产物来源不清楚，立即停止写入并报告。

## 3. 允许范围与禁止内容

允许进入本仓库：

- 公开页面与浏览器端静态资源；
- 已确认可公开的数据、清单和状态文件；
- 公开站点所需的域名与静态托管配置；
- 直接服务于本仓库维护的公开文档。

禁止进入本仓库：

- 私有算法、生产脚本、调度单元、容器与 NAS 运维配置；
- 原始行情、内部报告、未公开研究数据和用户数据；
- Token、密钥、证书、真实环境变量、内部地址和本机绝对路径；
- 日志、缓存、运行轨迹、会话标识、迁移证据和历史工作副本。

不新增符号链接、Git submodule 或 Git LFS，除非 kg 单独批准。

## 4. 修改原则

- 默认把本仓库视为发布产物仓，不在这里修复私有生成逻辑。
- 公开数据更新应来自已验证的私有构建链；只有明确的公开静态问题才允许直接修复。
- 数据文件、指纹文件名、manifest 与页面引用必须作为一个一致集合更新，禁止只替换其中一部分。
- 保持站点可由普通静态 HTTP 服务直接运行，不引入必须依赖后端服务的行为。
- 保持 `.nojekyll`、`CNAME`、根路径和现有公开入口稳定；改变域名、路由或托管方式需仓库所有者决定。
- 当前产品入口是 `index.html` 与 `macro/`。`realtime.html` 仅为历史遗留文件，已停止维护，不属于当前产品入口；除非 kg 明确要求处理历史兼容或删除事项，不得主动修改、品牌化、重设计、加入导航或纳入产品范围。
- 一次只处理一个主要问题，不顺手重排生成文件、格式化整站或升级无关依赖。

## 5. 验证要求

按变更影响执行：

- Markdown：检查事实、链接、公开边界和术语一致性。
- HTML/CSS/JavaScript：启动本地静态服务器，检查受影响页面、导航、资源加载、浏览器控制台和不同视口。
- JSON 与公开数据：检查语法，并核对 manifest、日期、指纹文件名和页面引用一致。
- 当前产品入口或数据集合变更：至少验证 `index.html` 及受影响的 `macro/` 页面；只有明确任务直接涉及历史兼容时，才验证 `realtime.html`。

本地预览可执行：

```bash
python3 -m http.server 8765
```

交付前至少执行：

```bash
git diff --check
git status --short
```

无法执行的浏览器、数据正确性或线上验证必须明确标记“未验证”，不得用文件存在或页面截图替代完整验证。

## 6. Git 与发布边界

- 同一工作区同一时间只允许一个写入责任人。
- 每次变更形成单一职责 commit，并能追溯到明确的静态修复或已验证产物来源。
- 不在历史副本、下载目录或临时发布目录继续开发。
- Git push、GitHub Pages 发布、DNS、域名、Cloudflare 与其他外部状态修改必须获得仓库所有者单独批准。
- 任何绕过自动发布器的人工 push，都必须与运行中的自动发布工作树做成一次连续操作：push 前确认自动发布任务空闲、工作树 clean 且本地 HEAD 等于当前远端；push 后在同一写锁保护下 fetch，并仅以 `ff-only` 同步到新远端，再确认本地 HEAD、远端跟踪引用一致且工作树 clean。
- 无权访问自动发布工作树、无法取得写锁或无法完成上述同步时，不得 push；可以保留本地 commit 等待协调。禁止用 reset、rebase 或 force push 消除分叉。
- 未经线上验证，不得声称已经发布或生产可用。

## 7. 完成报告

完成时至少说明：

```text
状态：COMPLETED / NEEDS_REVISION / BLOCKED
本次解决：
本次未做：
变更文件：
实际验证与退出结果：
公开数据或外部影响：
commit：
回滚入口：
已知风险与未验证项：
需要仓库所有者做什么：
```
