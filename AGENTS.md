# OneChart-Public 项目工作规则

本仓库是 OneChart 生成的公开部署镜像，不是产品开发或私有源码事实源。

## 1. 镜像边界

- 页面、样式、交互、公开数据和生成资产必须在私有 `OneChart` 中修改、验证并通过发布链生成，不在本仓库直接修复。
- 本仓库只接受已验证的发布产物，以及 kg 明确批准的镜像治理元数据。
- 禁止加入私有算法、生产脚本、调度与容器配置、原始数据、内部报告、用户数据、凭据、真实环境变量、内部地址、日志、会话信息或迁移证据。
- 数据文件、manifest、指纹文件名和页面引用必须作为同一批发布产物保持一致。

## 2. 写入门禁

写入前确认 Git 根、branch、commit、working tree 和产物来源。若工作区存在无法解释的改动、来源无法追溯、产物集合不完整或出现其他写入者，立即停止。

除受控发布同步或 kg 明确批准的镜像治理外，不得修改、commit 或生成新的仓库内容。

## 3. Git 与外部边界

- Git push、GitHub Pages、DNS、域名、Cloudflare 及其他外部状态修改必须获得 kg 单独批准；本地 commit 不代表发布授权。
- 人工 push 必须与 NAS 自动发布工作树在同一写锁内完成同步：push 前确认任务空闲、工作树 clean 且 HEAD 等于远端；push 后只以 `ff-only` 更新并再次核对。无法取得写锁或完成同步时不得 push。
- 禁止使用 reset、rebase 或 force push 消除生产发布分叉。
- 没有线上证据时不得声称已经发布或生产可用；未验证事项必须明确说明。
